{
  "name": "lingonika",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=10.11"
  },
  "description": "description",
  "scripts": {
    "postinstall": "npm --prefix client install && npm --prefix server install",
    "start-dev": "concurrently \"npm run --prefix server start\" \"npm run --prefix client start\"",
    "build": "npm run --prefix client/app build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "console": "npm run --prefix server console",
    "migrate": "npm run --prefix server migrate",
    "command": "npm run --prefix server command"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dmitrykologrivko/LINGONIKA.git"
  },
  "keywords": [
    "english"
  ],
  "author": "Dmitry Kologrivko",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/dmitrykologrivko/LINGONIKA/issues"
  },
  "homepage": "https://github.com/dmitrykologrivko/LINGONIKA#readme",
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
